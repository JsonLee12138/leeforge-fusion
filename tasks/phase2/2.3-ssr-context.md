# 任务 2.3: SSR 上下文管理

## 基本信息

- **任务编号**: 2.3
- **Phase**: 2 - SSR 渲染引擎
- **负责人**: 资深 Node 工程师
- **预计工时**: 1 天
- **优先级**: 🔴 P0
- **依赖**: 无

## 任务描述

管理 SSR 运行时上下文，包括请求、用户、配置等。

## 交付物

### 1. 核心文件

- `packages/framework/src/ssr/context.ts` - 上下文定义
- `packages/framework/src/ssr/context-factory.ts` - 上下文工厂

### 2. 功能要求

- [ ] 请求上下文封装
- [ ] 用户认证信息
- [ ] API 基础 URL
- [ ] QueryClient 实例
- [ ] 中间件上下文

## 实现

```typescript
// packages/framework/src/ssr/context.ts
export interface AppContext {
  request: Request;
  user?: User;
  API_BASE: string;
  queryClient: QueryClient;
  [key: string]: any;
}

export class ContextManager {
  static createContext(options: {
    request: Request;
    user?: User;
    API_BASE?: string;
  }): AppContext {
    return {
      request: options.request,
      user: options.user,
      API_BASE: options.API_BASE || "/api",
      queryClient: new QueryClient(),
    };
  }
}
```

## 验收标准

- [ ] 上下文类型安全
- [ ] 支持扩展
- [ ] 线程安全

---

**创建时间**: 2026-01-15  
**状态**: 待开始
