# 任务 3.1: API 路由扫描器

## 基本信息

- **任务编号**: 3.1
- **Phase**: 3 - API 路由系统
- **负责人**: 资深 Node 工程师
- **预计工时**: 1 天
- **优先级**: 🔴 P0
- **依赖**: 无

## 任务描述

扫描 `app/api/` 目录，识别 Hono 路由模块。

## 交付物

### 1. 核心文件

- `packages/framework/src/api/scanner.ts` - API 扫描器
- `packages/framework/src/api/types.ts` - API 路由类型

### 2. 功能要求

- [ ] 扫描 api 目录
- [ ] 识别 route.ts 文件
- [ ] 解析动态路由参数
- [ ] 提取 HTTP 方法
- [ ] 生成注册代码

## 实现

```typescript
// packages/framework/src/api/scanner.ts
export class APIScanner {
  async scan(apiDir: string): Promise<APIRoute[]> {
    const files = await glob(`${apiDir}/**/route.ts`);

    return files.map((file) => ({
      path: this.parsePath(file, apiDir),
      file,
      methods: this.extractMethods(file),
    }));
  }

  private parsePath(file: string, apiDir: string): string {
    // app/api/posts/[id]/route.ts → /posts/:id
  }

  private extractMethods(file: string): string[] {
    // 读取文件，提取 app.get/post/put/delete
  }
}
```

## 验收标准

- [ ] 正确解析路径
- [ ] 提取所有 HTTP 方法
- [ ] 支持动态路由

---

**创建时间**: 2026-01-15  
**状态**: 待开始
