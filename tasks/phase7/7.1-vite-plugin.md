# 任务 7.1: Vite 插件

## 基本信息

- **任务编号**: 7.1
- **Phase**: 7 - Vite 插件
- **负责人**: 资深 Node 工程师
- **预计工时**: 1 天
- **优先级**: 🟢 P2
- **依赖**: 任务 1.1, 1.2

## 任务描述

创建框架 Vite 插件。

## 交付物

### 1. 核心文件
- `packages/framework/src/vite-plugin.ts` - Vite 插件主文件

### 2. 功能要求
- [ ] 路由生成钩子
- [ ] 热重载处理
- [ ] SSR 构建配置

## 实现

```typescript
// packages/framework/src/vite-plugin.ts
export function frameworkPlugin(options: {
  appDir: string
  apiDir: string
}): Plugin {
  return {
    name: 'leeforge-plugin',
    
    async configResolved(config) {
      if (config.command === 'serve') {
        // 生成路由
        await generateRoutes(options.appDir)
      }
    },
    
    handleHotUpdate({ file, server }) {
      if (file.includes(options.appDir)) {
        // 重新生成路由
        generateRoutes(options.appDir)
        server.ws.send({ type: 'full-reload' })
      }
    }
  }
}
```

## 验收标准

- [ ] 插件正常工作
- [ ] 热重载正常
- [ ] 构建配置正确

---

**创建时间**: 2026-01-15  
**状态**: 待开始
