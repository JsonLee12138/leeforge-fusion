# 任务 6.4: Generate 命令

## 基本信息

- **任务编号**: 6.4
- **Phase**: 6 - CLI 工具
- **负责人**: 资深前端开发工程师
- **预计工时**: 2 天
- **优先级**: 🟡 P1
- **依赖**: 无

## 任务描述

实现代码生成命令。

## 交付物

### 1. 核心文件
- `packages/cli/src/commands/generate.ts` - Generate 命令
- `packages/cli/src/generators/page.ts` - 页面生成器
- `packages/cli/src/generators/api.ts` - API 生成器
- `packages/cli/src/generators/component.ts` - 组件生成器

### 2. 功能要求
- [ ] 页面生成
- [ ] API 生成
- [ ] 组件生成
- [ ] 模板系统

## 实现

```typescript
// packages/cli/src/commands/generate.ts
export async function generateCommand(
  type: 'page' | 'api' | 'component',
  name: string,
  options: any
) {
  const generators = {
    page: () => import('./generators/page').then(m => m.generatePage),
    api: () => import('./generators/api').then(m => m.generateAPI),
    component: () => import('./generators/component').then(m => m.generateComponent)
  }
  
  const generator = await generators[type]()
  await generator(name, options)
}
```

## 验收标准

- [ ] 所有生成器工作正常
- [ ] 模板正确
- [ ] 文件创建成功

---

**创建时间**: 2026-01-15  
**状态**: 待开始
