# 任务 7.2: 开发服务器集成

## 基本信息

- **任务编号**: 7.2
- **Phase**: 7 - Vite 插件
- **负责人**: 资深 Node 工程师
- **预计工时**: 1 天
- **优先级**: 🟢 P2
- **依赖**: 任务 2.1, 3.2

## 任务描述

集成 Vite 和 Hono 开发服务器。

## 交付物

### 1. 核心文件
- `packages/framework/src/dev-server.ts` - 开发服务器

### 2. 功能要求
- [ ] Vite 中间件
- [ ] Hono 服务器
- [ ] SSR 渲染
- [ ] API 路由

## 实现

```typescript
// packages/framework/src/dev-server.ts
export async function startDevServer(options: { port: number }) {
  const vite = await createServer({ /* ... */ })
  const app = new Hono()
  
  // API 路由
  app.route('/api', apiApp)
  
  // SSR
  app.get('*', async (c) => {
    const html = await render(c.req.url)
    return c.html(html)
  })
  
  app.listen(options.port)
}
```

## 验收标准

- [ ] 服务器启动正常
- [ ] API 可访问
- [ ] SSR 正常

---

**创建时间**: 2026-01-15  
**状态**: 待开始
