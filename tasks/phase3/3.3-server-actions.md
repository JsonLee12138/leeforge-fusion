# 任务 3.3: Server Actions 支持

## 基本信息

- **任务编号**: 3.3
- **Phase**: 3 - API 路由系统
- **负责人**: 资深 Node 工程师
- **预计工时**: 1 天
- **优先级**: 🟡 P1
- **依赖**: 无

## 任务描述

实现 `'use server'` 标记的 Server Actions。

## 交付物

### 1. 核心文件

- `packages/framework/src/server-actions/index.ts` - Server Actions 主文件
- `packages/framework/src/server-actions/rpc.ts` - RPC 调用

### 2. 功能要求

- [ ] 标记解析
- [ ] RPC 生成
- [ ] 类型安全
- [ ] 错误处理

## 实现

```typescript
// packages/framework/src/server-actions/index.ts
export function createAction<T extends (...args: any[]) => any>(fn: T): T {
  // 返回代理函数，调用时发送 RPC 请求
  return ((...args: any[]) => {
    return fetch("/api/__server_action__", {
      method: "POST",
      body: JSON.stringify({
        action: fn.name,
        args,
      }),
    }).then((r) => r.json());
  }) as T;
}
```

## 验收标准

- [ ] Server Actions 可用
- [ ] 类型安全
- [ ] 错误处理

---

**创建时间**: 2026-01-15  
**状态**: 待开始
