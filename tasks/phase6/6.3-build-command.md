# 任务 6.3: Build 命令

## 基本信息

- **任务编号**: 6.3
- **Phase**: 6 - CLI 工具
- **负责人**: 资深前端开发工程师
- **预计工时**: 1 天
- **优先级**: 🟡 P1
- **依赖**: 任务 7.1 (Vite 插件)

## 任务描述

实现 `leeforge build` 命令。

## 交付物

### 1. 核心文件
- `packages/cli/src/commands/build.ts` - Build 命令实现

### 2. 功能要求
- [ ] 客户端构建
- [ ] 服务端构建
- [ ] 静态生成
- [ ] 资源优化

## 实现

```typescript
// packages/cli/src/commands/build.ts
export async function buildCommand() {
  const { build } = await import('vite')
  
  // 客户端构建
  await build({
    build: {
      outDir: 'dist/client',
      rollupOptions: { /* ... */ }
    }
  })
  
  // 服务端构建
  await build({
    build: {
      outDir: 'dist/server',
      ssr: true
    }
  })
}
```

## 验收标准

- [ ] 构建成功
- [ ] 输出正确
- [ ] 无错误

---

**创建时间**: 2026-01-15  
**状态**: 待开始
