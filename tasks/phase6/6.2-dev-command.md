# 任务 6.2: Dev 命令

## 基本信息

- **任务编号**: 6.2
- **Phase**: 6 - CLI 工具
- **负责人**: 资深前端开发工程师
- **预计工时**: 1 天
- **优先级**: 🟡 P1
- **依赖**: 任务 7.2 (开发服务器)

## 任务描述

实现 `leeforge dev` 命令。

## 交付物

### 1. 核心文件
- `packages/cli/src/commands/dev.ts` - Dev 命令实现

### 2. 功能要求
- [ ] 启动开发服务器
- [ ] 端口配置
- [ ] 热重载支持
- [ ] 错误处理

## 实现

```typescript
// packages/cli/src/commands/dev.ts
export async function devCommand(options: { port?: number } = {}) {
  const { startDevServer } = await import('@leeforge/framework')
  
  try {
    await startDevServer({
      port: options.port || 3000
    })
  } catch (error) {
    console.error('Failed to start dev server:', error)
    process.exit(1)
  }
}
```

## 验收标准

- [ ] 服务器正常启动
- [ ] 端口配置生效
- [ ] 错误处理完善

---

**创建时间**: 2026-01-15  
**状态**: 待开始
