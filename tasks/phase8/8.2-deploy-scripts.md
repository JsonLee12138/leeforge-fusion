# 任务 8.2: 部署脚本

## 基本信息

- **任务编号**: 8.2
- **Phase**: 8 - 生产服务器
- **负责人**: 资深 Node 工程师
- **预计工时**: 1 天
- **优先级**: 🟢 P2
- **依赖**: 无

## 任务描述

创建部署辅助脚本。

## 交付物

### 1. 核心文件
- `packages/framework/scripts/deploy.ts` - 部署脚本
- `packages/framework/scripts/Dockerfile` - Docker 模板

### 2. 功能要求
- [ ] Docker 配置生成
- [ ] 环境变量检查
- [ ] 部署前验证

## 实现

```typescript
// packages/framework/scripts/deploy.ts
export function generateDockerfile(): string {
  return `
FROM oven/bun:1
WORKDIR /app
COPY . .
RUN bun install && bun build
EXPOSE 3000
CMD ["bun", "start"]
`
}

export async function checkProductionBuild(): Promise<boolean> {
  // 检查 dist 目录是否存在
  // 检查必要文件
  // 返回验证结果
}
```

## 验收标准

- [ ] Dockerfile 正确
- [ ] 检查脚本有效
- [ ] 文档完整

---

**创建时间**: 2026-01-15  
**状态**: 待开始
