# 任务 3.2: API 路由注册器

## 基本信息

- **任务编号**: 3.2
- **Phase**: 3 - API 路由系统
- **负责人**: 资深 Node 工程师
- **预计工时**: 1 天
- **优先级**: 🔴 P0
- **依赖**: 任务 3.1 (API 扫描器)

## 任务描述

自动注册 API 路由到 Hono 应用。

## 交付物

### 1. 核心文件

- `packages/framework/src/api/registry.ts` - API 注册器
- `packages/framework/src/api/middleware.ts` - API 中间件

### 2. 功能要求

- [ ] 创建 Hono 应用
- [ ] 自动挂载路由
- [ ] 支持中间件
- [ ] 错误处理

## 实现

```typescript
// packages/framework/src/api/registry.ts
export class APIRegistry {
  private app: Hono;

  constructor() {
    this.app = new Hono();
  }

  async register(routes: APIRoute[]): Promise<Hono> {
    for (const route of routes) {
      const module = await import(route.file);
      this.app.route(route.path, module.default);
    }
    return this.app;
  }
}
```

## 验收标准

- [ ] 路由正确挂载
- [ ] 中间件支持
- [ ] 错误处理

---

**创建时间**: 2026-01-15  
**状态**: 待开始
