# 任务 8.1: 生产服务器

## 基本信息

- **任务编号**: 8.1
- **Phase**: 8 - 生产服务器
- **负责人**: 资深 Node 工程师
- **预计工时**: 1 天
- **优先级**: 🟢 P2
- **依赖**: 任务 7.2

## 任务描述

实现生产环境服务器。

## 交付物

### 1. 核心文件
- `packages/framework/src/prod-server.ts` - 生产服务器

### 2. 功能要求
- [ ] 静态文件服务
- [ ] API 路由加载
- [ ] SSR 渲染
- [ ] 性能优化

## 实现

```typescript
// packages/framework/src/prod-server.ts
export async function startProdServer(options: { port: number }) {
  const app = new Hono()
  
  // 静态资源
  app.use('/assets/*', serveStatic({ root: './dist/client' }))
  
  // API 路由
  const apiRoutes = await import('./dist/server/api-routes.js')
  app.route('/api', apiRoutes.default)
  
  // SSR
  app.get('*', async (c) => {
    const { render } = await import('./dist/server/render.js')
    const html = await render(c.req.url)
    return c.html(html)
  })
  
  app.listen(options.port)
}
```

## 验收标准

- [ ] 生产服务器正常
- [ ] 静态资源可访问
- [ ] SSR 正常

---

**创建时间**: 2026-01-15  
**状态**: 待开始
